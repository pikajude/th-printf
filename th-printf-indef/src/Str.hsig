signature Str
    ( Str
    , Chr
    , Index
    , head
    , justifyLeft
    , justifyRight
    , length
    , empty
    , chr
    , singleton
    , elem
    , cons'

    , showDecimal
    ) where

import Prelude (Char, Integral, Show, Bool, Maybe)

data Chr

data Str

data Index

chr :: Char -> Chr

showDecimal :: (Integral a, Show a) => a -> Str
