signature Str
    ( Str
    , Chr
    , Index
    , head
    , justifyLeft
    , justifyRight
    , length
    , empty
    , chr

    , showDecimal
    ) where

import Prelude (Char, Integral, Show, Bool)

data Chr

data Str

data Index

chr :: Char -> Chr

showDecimal :: (Integral a, Show a) => a -> Str
